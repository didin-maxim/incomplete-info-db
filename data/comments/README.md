# comments

Комментарии ревьюеров хранятся отдельными JSON-совместимыми `.yaml` файлами.

Цели:

- `target.type = "problem"` и `target.problem_id = "..."`
- `target.type = "architecture"`

Основные виды:

- `bug_report`
- `alternative_strategy`
- `related_connection`
- `architecture`
